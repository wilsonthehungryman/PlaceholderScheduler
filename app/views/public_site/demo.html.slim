.jumbotron
  - if @contents.empty?
    h1 PublicSite#demo
    p Find me in app/views/public_site/demo.html.slim
  - else
    - @contents.each do |content|
      div = render html: content.content.html_safe

.jumbotron
  - if @contents.empty?
    h1 PublicSite#contact
    p Find me in app/views/public_site/contact.html.slim
  - else
    - @contents.each do |content|
      div = render html: content.content.html_safe
